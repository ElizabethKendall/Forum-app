import { FilterForumTablePipe } from './filter-forum-table.pipe';

describe('FilterForumTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForumTablePipe();
    expect(pipe).toBeTruthy();
  });
});
